
Hello World

How are you??

I am fine and you?

Well the check is confirmed.

The sublime editor is used for editing the 
file in local git repository by opening the
repo and the file in sublime editor we edit
the files and then we can save them.

AFter the file is saved we can just exit the
editor and then push to the origin and then create
a pull request from the origin or merge it directly
into the main branch and commit the changes.origin

Then we can view the changes and the versions of 
the changes both in local and in shared repository
(i.e., Git using browser).


Done with testing with local, sublime editor and 
shared repository.  